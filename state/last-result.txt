**Changes**
`POST /api/open/notes` in [route.ts](/Users/stevenna/WebstormProjects/minakeep/src/app/api/open/notes/route.ts) now accepts keyed JSON note creation, validates `title` and `markdown`, supports optional `isPublished`, creates the note under the configured owner, and returns `201` with the note identity plus `ownerUrl` and `publicUrl`. I extracted the shared note-create lifecycle into [runtime.ts](/Users/stevenna/WebstormProjects/minakeep/src/features/notes/runtime.ts) and switched [actions.ts](/Users/stevenna/WebstormProjects/minakeep/src/app/app/notes/actions.ts) to use it, so the API path reuses the existing slugging, publish, enrichment scheduling, and revalidation behavior instead of creating a second workflow.

I added focused coverage in [note-api.spec.ts](/Users/stevenna/WebstormProjects/minakeep/tests/e2e/note-api.spec.ts) and updated the route unit coverage in [external-note-api-auth.test.ts](/Users/stevenna/WebstormProjects/minakeep/tests/unit/external-note-api-auth.test.ts). The note-api E2E tests now clean up their created notes so repeated targeted runs do not contaminate the shared SQLite state. I also updated the relevant docs and task log, and wrote the operator handoff to [last-result.txt](/Users/stevenna/WebstormProjects/minakeep/state/last-result.txt).

**Checks**
`npm run test:e2e -- --grep @note-api` passed. `npm run verify` passed. The only notable residual is the existing non-blocking Turbopack NFT tracing warning during `next build` on `/api/open/notes`.